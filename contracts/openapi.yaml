openapi: 3.1.0
info:
  title: Play&Say API Gateway
  description: Public contract for the Play&Say API Gateway.
  version: 0.1.0
servers:
- url: /api
  description: Play&Say web-app API proxy
paths:
  /users/me/profile:
    get:
      tags:
      - Users
      summary: Current application user profile
      description: Returns the app-level profile for the current Keycloak user.
      operationId: getMyUserProfile
      responses:
        "200":
          description: Current app-level user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfileResponse"
        "401":
          description: Missing or invalid bearer token
      security:
      - bearerAuth: []
    put:
      tags:
      - Users
      summary: Update current application user profile
      description: Updates editable profile fields for the current Keycloak user.
      operationId: updateMyUserProfile
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserProfileRequest"
        required: true
      responses:
        "200":
          description: Updated app-level user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfileResponse"
        "400":
          description: Invalid profile payload
        "401":
          description: Missing or invalid bearer token
      security:
      - bearerAuth: []
    delete:
      tags:
      - Users
      summary: Delete current application user profile
      description: Deletes editable app-level profile data for the current Keycloak
        user.
      operationId: deleteMyUserProfile
      responses:
        "204":
          description: Current app-level user profile deleted
        "401":
          description: Missing or invalid bearer token
      security:
      - bearerAuth: []
  /schedule/lessons/{lessonId}:
    get:
      tags:
      - Schedule
      summary: Get scheduled lesson
      description: Returns a scheduled lesson visible to the current user.
      operationId: getScheduledLesson
      parameters:
      - name: lessonId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Scheduled lesson
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduledLessonResponse"
        "401":
          description: Missing or invalid bearer token
        "404":
          description: Scheduled lesson not found
      security:
      - bearerAuth: []
    put:
      tags:
      - Schedule
      summary: Update scheduled lesson
      description: Updates a calendar lesson. Requires TEACHER or ADMIN role.
      operationId: updateScheduledLesson
      parameters:
      - name: lessonId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduledLessonRequest"
        required: true
      responses:
        "200":
          description: Scheduled lesson updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduledLessonResponse"
        "400":
          description: Invalid scheduled lesson payload
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage schedule
        "404":
          description: Scheduled lesson not found
      security:
      - bearerAuth: []
    delete:
      tags:
      - Schedule
      summary: Delete scheduled lesson
      description: Deletes a calendar lesson. Requires TEACHER or ADMIN role.
      operationId: deleteScheduledLesson
      parameters:
      - name: lessonId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Scheduled lesson deleted
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage schedule
        "404":
          description: Scheduled lesson not found
      security:
      - bearerAuth: []
  /materials/{materialId}:
    get:
      tags:
      - Materials
      summary: Get lesson material
      description: Returns a visible lesson material.
      operationId: getMaterial
      parameters:
      - name: materialId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Lesson material
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonMaterialResponse"
        "401":
          description: Missing or invalid bearer token
        "404":
          description: Material not found
      security:
      - bearerAuth: []
    put:
      tags:
      - Materials
      summary: Update lesson material
      description: Updates a structured lesson material. Requires material owner or
        ADMIN role.
      operationId: updateMaterial
      parameters:
      - name: materialId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LessonMaterialRequest"
        required: true
      responses:
        "200":
          description: Lesson material updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonMaterialResponse"
        "400":
          description: Invalid material payload
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot edit material
        "404":
          description: Material not found
      security:
      - bearerAuth: []
    delete:
      tags:
      - Materials
      summary: Archive lesson material
      description: Archives a lesson material. Requires material owner or ADMIN role.
      operationId: archiveMaterial
      parameters:
      - name: materialId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Lesson material archived
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot archive material
        "404":
          description: Material not found
      security:
      - bearerAuth: []
  /courses/{courseId}:
    get:
      tags:
      - Courses
      summary: Get course
      description: Returns a single course. Unpublished courses are visible only to
        teachers/admins.
      operationId: getCourse
      parameters:
      - name: courseId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Course
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseResponse"
        "401":
          description: Missing or invalid bearer token
        "404":
          description: Course not found
      security:
      - bearerAuth: []
    put:
      tags:
      - Courses
      summary: Update course
      description: Updates a course. Requires TEACHER or ADMIN role.
      operationId: updateCourse
      parameters:
      - name: courseId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseRequest"
        required: true
      responses:
        "200":
          description: Course updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseResponse"
        "400":
          description: Invalid course payload
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage courses
        "404":
          description: Course not found
      security:
      - bearerAuth: []
    delete:
      tags:
      - Courses
      summary: Delete course
      description: Deletes a course and its draft lesson templates. Requires TEACHER
        or ADMIN role.
      operationId: deleteCourse
      parameters:
      - name: courseId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Course deleted
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage courses
        "404":
          description: Course not found
      security:
      - bearerAuth: []
  /courses/{courseId}/lessons/{lessonId}:
    put:
      tags:
      - Courses
      summary: Update course lesson
      description: Updates a lesson template inside a course. Requires TEACHER or
        ADMIN role.
      operationId: updateCourseLesson
      parameters:
      - name: courseId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: lessonId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseLessonRequest"
        required: true
      responses:
        "200":
          description: Course lesson updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseLessonResponse"
        "400":
          description: Invalid course lesson payload
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage courses
        "404":
          description: Course lesson not found
      security:
      - bearerAuth: []
    delete:
      tags:
      - Courses
      summary: Delete course lesson
      description: Deletes a lesson template inside a course. Requires TEACHER or
        ADMIN role.
      operationId: deleteCourseLesson
      parameters:
      - name: courseId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: lessonId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Course lesson deleted
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage courses
        "404":
          description: Course lesson not found
      security:
      - bearerAuth: []
  /schedule/lessons:
    get:
      tags:
      - Schedule
      summary: List scheduled lessons
      description: "Returns scheduled lessons. Teachers/admins see all, students see\
        \ lessons where they are participants."
      operationId: listScheduledLessons
      responses:
        "200":
          description: Scheduled lessons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ScheduledLessonResponse"
        "401":
          description: Missing or invalid bearer token
      security:
      - bearerAuth: []
    post:
      tags:
      - Schedule
      summary: Create scheduled lesson
      description: Creates a calendar lesson. Requires TEACHER or ADMIN role.
      operationId: createScheduledLesson
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScheduledLessonRequest"
        required: true
      responses:
        "201":
          description: Scheduled lesson created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduledLessonResponse"
        "400":
          description: Invalid scheduled lesson payload
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage schedule
      security:
      - bearerAuth: []
  /schedule/lessons/{lessonId}/room-token:
    post:
      tags:
      - Schedule
      summary: Create scheduled lesson video token
      description: Returns a short-lived LiveKit join token for a scheduled lesson
        visible to the current user.
      operationId: createScheduledLessonRoomToken
      parameters:
      - name: lessonId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: LiveKit room token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LiveKitRoomTokenResponse"
        "401":
          description: Missing or invalid bearer token
        "404":
          description: Scheduled lesson not found
        "503":
          description: LiveKit is not configured
      security:
      - bearerAuth: []
  /materials:
    get:
      tags:
      - Materials
      summary: List lesson materials
      description: Teachers/admins see their materials and published public materials.
        Students see published public materials.
      operationId: listMaterials
      responses:
        "200":
          description: Lesson materials
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LessonMaterialResponse"
        "401":
          description: Missing or invalid bearer token
      security:
      - bearerAuth: []
    post:
      tags:
      - Materials
      summary: Create lesson material
      description: Creates a structured lesson material. Requires TEACHER or ADMIN
        role.
      operationId: createMaterial
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LessonMaterialRequest"
        required: true
      responses:
        "201":
          description: Lesson material created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonMaterialResponse"
        "400":
          description: Invalid material payload
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage materials
      security:
      - bearerAuth: []
  /materials/{materialId}/assets:
    get:
      tags:
      - Materials
      summary: List material assets
      operationId: listMaterialAssets
      parameters:
      - name: materialId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MaterialAssetResponse"
      security:
      - bearerAuth: []
    post:
      tags:
      - Materials
      summary: Create material asset reference
      operationId: createMaterialAsset
      parameters:
      - name: materialId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MaterialAssetRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MaterialAssetResponse"
      security:
      - bearerAuth: []
  /materials/ai-draft:
    post:
      tags:
      - Materials
      summary: Draft lesson material with AI
      description: "Returns a structured Play&Say material draft from the configured\
        \ AI provider, or deterministic stub when AI is disabled."
      operationId: draftMaterialWithAi
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MaterialAiDraftRequest"
        required: true
      responses:
        "200":
          description: Draft material
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonMaterialDraftResponse"
        "400":
          description: Invalid draft prompt
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage materials
      security:
      - bearerAuth: []
  /courses:
    get:
      tags:
      - Courses
      summary: List courses
      description: Returns published courses for students and all courses for teachers/admins.
      operationId: listCourses
      responses:
        "200":
          description: Courses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CourseResponse"
        "401":
          description: Missing or invalid bearer token
      security:
      - bearerAuth: []
    post:
      tags:
      - Courses
      summary: Create course
      description: Creates a course. Requires TEACHER or ADMIN role.
      operationId: createCourse
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseRequest"
        required: true
      responses:
        "201":
          description: Course created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseResponse"
        "400":
          description: Invalid course payload
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage courses
      security:
      - bearerAuth: []
  /courses/{courseId}/lessons:
    get:
      tags:
      - Courses
      summary: List course lessons
      description: Returns lesson templates inside a course. Unpublished courses are
        visible only to teachers/admins.
      operationId: listCourseLessons
      parameters:
      - name: courseId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Course lessons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CourseLessonResponse"
        "401":
          description: Missing or invalid bearer token
        "404":
          description: Course not found
      security:
      - bearerAuth: []
    post:
      tags:
      - Courses
      summary: Create course lesson
      description: Creates a lesson template inside a course. Requires TEACHER or
        ADMIN role.
      operationId: createCourseLesson
      parameters:
      - name: courseId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseLessonRequest"
        required: true
      responses:
        "201":
          description: Course lesson created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseLessonResponse"
        "400":
          description: Invalid course lesson payload
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user cannot manage courses
        "404":
          description: Course not found
      security:
      - bearerAuth: []
  /users/students:
    get:
      tags:
      - Users
      summary: List student user profiles
      description: Returns known app-level student profiles. Requires TEACHER or ADMIN
        role.
      operationId: listStudentProfiles
      responses:
        "200":
          description: Known student user profiles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserProfileResponse"
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user is not a teacher/admin
      security:
      - bearerAuth: []
  /schedule/lessons/{lessonId}/material:
    get:
      tags:
      - Materials
      summary: Get scheduled lesson material
      description: Returns the material attached directly to a scheduled lesson or
        inherited from its lesson template.
      operationId: getScheduledLessonMaterial
      parameters:
      - name: lessonId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Scheduled lesson material
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonMaterialResponse"
        "401":
          description: Missing or invalid bearer token
        "404":
          description: Scheduled lesson or material not found
      security:
      - bearerAuth: []
  /me:
    get:
      tags:
      - Auth
      summary: Current user profile
      description: Returns the current JWT profile and Play&Say application roles.
      operationId: getMe
      responses:
        "200":
          description: Current user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"
        "401":
          description: Missing or invalid bearer token
      security:
      - bearerAuth: []
  /hello:
    get:
      tags:
      - Dev
      summary: Dev health message
      description: Returns a small public payload used by smoke tests.
      operationId: getHello
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HelloResponse"
  /admin/users:
    get:
      tags:
      - Users
      summary: List application user profiles
      description: Returns known app-level user profiles. Requires the ADMIN role.
      operationId: listUserProfiles
      responses:
        "200":
          description: Known app-level user profiles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserProfileResponse"
        "401":
          description: Missing or invalid bearer token
        "403":
          description: Current user is not an admin
      security:
      - bearerAuth: []
  /materials/{materialId}/assets/{assetId}:
    delete:
      tags:
      - Materials
      summary: Delete material asset reference
      operationId: deleteMaterialAsset
      parameters:
      - name: materialId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: assetId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: OK
      security:
      - bearerAuth: []
components:
  schemas:
    UpdateUserProfileRequest:
      type: object
      properties:
        displayName:
          type:
          - string
          - "null"
          maxLength: 120
        locale:
          type:
          - string
          - "null"
          maxLength: 16
        timezone:
          type:
          - string
          - "null"
          maxLength: 64
        learningGoal:
          type:
          - string
          - "null"
          maxLength: 500
    UserProfileResponse:
      type: object
      properties:
        subject:
          type: string
        username:
          type:
          - string
          - "null"
        email:
          type:
          - string
          - "null"
        name:
          type:
          - string
          - "null"
        roles:
          type: array
          items:
            type: string
        displayName:
          type:
          - string
          - "null"
        locale:
          type:
          - string
          - "null"
        timezone:
          type:
          - string
          - "null"
        learningGoal:
          type:
          - string
          - "null"
        updatedAt:
          type: string
          format: date-time
      required:
      - roles
      - subject
      - updatedAt
    ScheduledLessonRequest:
      type: object
      properties:
        lessonTemplateId:
          type:
          - string
          - "null"
          format: uuid
        materialId:
          type:
          - string
          - "null"
          format: uuid
        scheduledStart:
          type:
          - string
          - "null"
          format: date-time
        scheduledEnd:
          type:
          - string
          - "null"
          format: date-time
        status:
          type: string
          enum:
          - SCHEDULED
          - IN_PROGRESS
          - COMPLETED
          - CANCELLED
        type:
          type: string
          enum:
          - INDIVIDUAL
          - GROUP
        participantSubjects:
          type: array
          items:
            type: string
      required:
      - participantSubjects
      - status
      - type
    ScheduledLessonParticipantResponse:
      type: object
      properties:
        subject:
          type: string
        username:
          type:
          - string
          - "null"
        displayName:
          type:
          - string
          - "null"
        attendanceStatus:
          type:
          - string
          - "null"
      required:
      - subject
    ScheduledLessonResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        lessonTemplateId:
          type:
          - string
          - "null"
          format: uuid
        materialId:
          type:
          - string
          - "null"
          format: uuid
        materialTitle:
          type:
          - string
          - "null"
        courseId:
          type:
          - string
          - "null"
          format: uuid
        courseTitle:
          type:
          - string
          - "null"
        lessonTitle:
          type:
          - string
          - "null"
        teacherSubject:
          type:
          - string
          - "null"
        teacherName:
          type:
          - string
          - "null"
        scheduledStart:
          type:
          - string
          - "null"
          format: date-time
        scheduledEnd:
          type:
          - string
          - "null"
          format: date-time
        status:
          type: string
        type:
          type: string
        livekitRoomName:
          type:
          - string
          - "null"
        participants:
          type: array
          items:
            $ref: "#/components/schemas/ScheduledLessonParticipantResponse"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
      - createdAt
      - id
      - participants
      - status
      - type
      - updatedAt
    JsonNode: {}
    LessonMaterialRequest:
      type: object
      properties:
        title:
          type: string
          maxLength: 160
        description:
          type:
          - string
          - "null"
          maxLength: 2000
        language:
          type: string
          maxLength: 16
        cefrLevel:
          type: string
          enum:
          - A1
          - A2
          - B1
          - B2
          - C1
          - C2
        visibility:
          type: string
          enum:
          - PRIVATE
          - PUBLIC
        status:
          type: string
          enum:
          - DRAFT
          - PUBLISHED
          - ARCHIVED
        document:
          oneOf:
          - $ref: "#/components/schemas/JsonNode"
          - type: "null"
        sourceMeta:
          oneOf:
          - $ref: "#/components/schemas/JsonNode"
          - type: "null"
        scoringRubric:
          oneOf:
          - $ref: "#/components/schemas/JsonNode"
          - type: "null"
      required:
      - cefrLevel
      - language
      - status
      - title
      - visibility
    LessonMaterialResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        ownerTeacherUserId:
          type:
          - string
          - "null"
          format: uuid
        ownerTeacherSubject:
          type:
          - string
          - "null"
        ownerTeacherName:
          type:
          - string
          - "null"
        title:
          type: string
        description:
          type:
          - string
          - "null"
        language:
          type: string
        cefrLevel:
          type: string
        visibility:
          type: string
        status:
          type: string
        document:
          $ref: "#/components/schemas/JsonNode"
        sourceMeta:
          $ref: "#/components/schemas/JsonNode"
        scoringRubric:
          $ref: "#/components/schemas/JsonNode"
        blockCount:
          type: integer
          format: int32
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
      - blockCount
      - cefrLevel
      - createdAt
      - document
      - id
      - language
      - scoringRubric
      - sourceMeta
      - status
      - title
      - updatedAt
      - visibility
    CourseRequest:
      type: object
      properties:
        title:
          type: string
          maxLength: 160
        description:
          type:
          - string
          - "null"
          maxLength: 2000
        level:
          type:
          - string
          - "null"
          maxLength: 16
        language:
          type: string
          maxLength: 16
        isPublished:
          type: boolean
      required:
      - isPublished
      - language
      - title
    CourseResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type:
          - string
          - "null"
        level:
          type:
          - string
          - "null"
        language:
          type: string
        createdByUserId:
          type:
          - string
          - "null"
          format: uuid
        isPublished:
          type: boolean
        lessonCount:
          type: integer
          format: int32
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
      - createdAt
      - id
      - isPublished
      - language
      - lessonCount
      - title
      - updatedAt
    CourseLessonRequest:
      type: object
      properties:
        title:
          type: string
          maxLength: 160
        orderIndex:
          type:
          - integer
          - "null"
          format: int32
        plannedDurationMin:
          type:
          - integer
          - "null"
          format: int32
        materialId:
          type:
          - string
          - "null"
          format: uuid
      required:
      - title
    CourseLessonResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        courseId:
          type: string
          format: uuid
        title:
          type: string
        orderIndex:
          type:
          - integer
          - "null"
          format: int32
        plannedDurationMin:
          type:
          - integer
          - "null"
          format: int32
        materialId:
          type:
          - string
          - "null"
          format: uuid
        materialTitle:
          type:
          - string
          - "null"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
      - courseId
      - createdAt
      - id
      - title
      - updatedAt
    LiveKitRoomTokenResponse:
      type: object
      properties:
        serverUrl:
          type: string
        token:
          type: string
        roomName:
          type: string
        identity:
          type: string
        expiresAt:
          type: string
          format: date-time
      required:
      - expiresAt
      - identity
      - roomName
      - serverUrl
      - token
    MaterialAssetRequest:
      type: object
      properties:
        kind:
          type: string
          enum:
          - IMAGE
          - GENERATED_IMAGE
          - DOCUMENT_SCAN
          - VIDEO_EMBED
          - EXTERNAL_SOURCE
          - AUDIO_NOTE
        storageKey:
          type:
          - string
          - "null"
        externalUrl:
          type:
          - string
          - "null"
        provider:
          type: string
          enum:
          - YOUTUBE
          - VK
          - RUTUBE
          - URL
          - UPLOAD
          - AI
        metadata:
          oneOf:
          - $ref: "#/components/schemas/JsonNode"
          - type: "null"
      required:
      - kind
      - provider
    MaterialAssetResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        materialId:
          type: string
          format: uuid
        kind:
          type: string
        storageKey:
          type:
          - string
          - "null"
        externalUrl:
          type:
          - string
          - "null"
        provider:
          type: string
        metadata:
          $ref: "#/components/schemas/JsonNode"
        createdAt:
          type: string
          format: date-time
      required:
      - createdAt
      - id
      - kind
      - materialId
      - metadata
      - provider
    MaterialAiDraftRequest:
      type: object
      properties:
        title:
          type:
          - string
          - "null"
          maxLength: 160
        prompt:
          type: string
          maxLength: 4000
        language:
          type: string
          maxLength: 16
        cefrLevel:
          type:
          - string
          - "null"
          enum:
          - A1
          - A2
          - B1
          - B2
          - C1
          - C2
      required:
      - language
      - prompt
    LessonMaterialDraftResponse:
      type: object
      properties:
        title:
          type: string
        description:
          type:
          - string
          - "null"
        language:
          type: string
        cefrLevel:
          type: string
        visibility:
          type: string
        status:
          type: string
        document:
          $ref: "#/components/schemas/JsonNode"
        sourceMeta:
          $ref: "#/components/schemas/JsonNode"
        scoringRubric:
          $ref: "#/components/schemas/JsonNode"
      required:
      - cefrLevel
      - document
      - language
      - scoringRubric
      - sourceMeta
      - status
      - title
      - visibility
    MeResponse:
      type: object
      properties:
        subject:
          type: string
        username:
          type:
          - string
          - "null"
        email:
          type:
          - string
          - "null"
        name:
          type:
          - string
          - "null"
        roles:
          type: array
          items:
            type: string
      required:
      - roles
      - subject
    HelloResponse:
      type: object
      properties:
        service:
          type: string
        message:
          type: string
        timestamp:
          type: string
          format: date-time
      required:
      - message
      - service
      - timestamp
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
