# Shared UI

Shared React components will move here once the single SPA starts needing reusable lesson, editor, and dashboard primitives.

The package also owns tiny shared frontend config exports that must not be duplicated between `web-app` and `keyboard-app`, starting with `publicSiteUrl` for the root Honey School website.
